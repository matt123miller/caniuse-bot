# TODO

Migrate to bun.
Migrate al code to ESM.
Migrate to turborepo for monorepo management.
Update various build tooling (TS, eslint, prettier etc.) to more modern versions, each config as a package in the monorepo.
Investigate whether I need a DB to manage persistent user signups / links with their chat apps.
Work out how to hook up the debugger for each app.
Create discord bot as app in monorepo.
Create slack bot as app in monorepo.